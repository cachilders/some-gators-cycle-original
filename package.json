{
  "name": "some-gators-cycle",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "cycle-scripts": "1.1.0"
  },
  "dependencies": {
    "@cycle/dom": "^17.1.0",
    "@cycle/run": "^3.1.0",
    "serve": "^5.1.4",
    "xstream": "^10.5.0"
  },
  "scripts": {
    "start": "npm run build && serve build",
    "test": "cycle-scripts test",
    "build": "cycle-scripts build",
    "dev": "cycle-scripts start",
    "eject": "cycle-scripts eject"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "eslintConfig": {
    "parserOptions": {
      "ecmaVersion": 6,
      "sourceType": "module",
      "ecmaFeatures": {
          "jsx": false
      }
    },
    "rules": {
      "semi": 2
    }
  }
}
